/**
 * Explanation:
 * Placeholder for a `useFetch` question implementation.
 * Add the actual hook/problem solution below when ready.
 *
 * Interview Thinking Process:
 * 1) Clarify requirements (loading, error, retry, cancellation).
 * 2) Choose state shape for data/loading/error.
 * 3) Handle async lifecycle and cleanup correctly.
 * 4) Prevent stale updates/race conditions.
 * 5) Discuss extensibility (caching, pagination, retries).
 */

export {}

